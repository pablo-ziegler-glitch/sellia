rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Modo temporal para destrabar desarrollo/integración.
    // TODO: reintroducir reglas por tenant/rol de forma incremental.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
