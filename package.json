{
  "name": "sellia-apphosting-web",
  "version": "1.0.0",
  "private": true,
  "description": "Servidor Node.js para Firebase App Hosting que publica el sitio estático de /public",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node apphosting-server.js",
    "test:firestore-rules": "firebase emulators:exec --only firestore \"node --test tests/firestore.rules.emulator.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.2",
    "firebase-tools": "^13.30.0"
  }
}
